(defvar rtags-pre-extensions
  '(
    company
    auto-complete
    )
  "List of all extensions to load before the packages.")

(defvar rtags-post-extensions
  '(
    ;; post extension rtags go here
    )
  "List of all extensions to load after the packages.")
